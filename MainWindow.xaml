<Window x:Class="UnitDesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Widget" Height="300" Width="400"
        AllowsTransparency="True" WindowStyle="None"
        Background="Transparent" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove">

    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Close" Click="MenuItem_Close_Click"/>
            <MenuItem Header="Reset Position" Click="MenuItem_Reset_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border CornerRadius="10"  Padding="15">
        <StackPanel>
            <!--<TextBlock x:Name="ClockText" FontSize="28" Foreground="White" HorizontalAlignment="Center"/>-->
            <TextBlock x:Name="SystemInfoText" FontSize="14" Foreground="#AAA" HorizontalAlignment="Center"/>
            <Separator Margin="5"/>
            <TextBlock Text="Disk Usage" FontSize="16" Foreground="#FFF" HorizontalAlignment="Center" Margin="0,5"/>
            <ListBox x:Name="DiskList" Background="Transparent" BorderThickness="0" Foreground="#DDD"/>
            <Separator Margin="5"/>
            <TextBlock Text="GPU Usage" FontSize="16" Foreground="#FFF" HorizontalAlignment="Center" Margin="0,5"/>
            <TextBlock x:Name="GpuInfoText" FontSize="14" Foreground="#DDD" TextWrapping="Wrap"/>

        </StackPanel>
    </Border>
</Window>
